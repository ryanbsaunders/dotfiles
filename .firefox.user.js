/* firefox preferences */


/* startup */

/* don't check if we are the default browser */
user_pref("browser.shell.checkDefaultBrowser", false);

/* set the startup page to a blank page */
user_pref("browser.startup.page", 0);
